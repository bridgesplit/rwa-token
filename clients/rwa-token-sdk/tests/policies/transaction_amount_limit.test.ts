// setup txn amount limit policy and check all values match
// update txn amount limit policy policy and check all values match
// get user to max amount that policy enforce
// check txn amount limit policy isnt being enforced for users with skip level
// check txn amount limit policy can be removed and no data is being left behind